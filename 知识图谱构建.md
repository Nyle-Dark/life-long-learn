# 数据准备：json格式
## boss数据（岗）

```
{
  "岗位ID": "06d49e273d96be8403153tu1GFVW",
  "岗位名称": "云计算网络研发工程师",
  "城市": "武汉",
  "门槛要求": {
    "学历": "本科",
    "经验": "1-3年"
  },
  "薪资特征": {
    "原始区间": "15-22K",
    "薪资下限": 15,
    "薪资上限": 22
  },
  "核心硬技能 (Hard_Skills)": [
    "Golang",
    "C++",
    "Kubernetes",
    "Docker",
    "Linux",
    "云原生"
  ],
  "宽泛领域 (Domains)": [
    "数据库",
    "网络技术"
  ]
}
```


## 课程数据（课）

```
{
  "课程名称": "云计算平台管理",
  "任务模块": "Nova计算服务组件安装",
  "知识点实体 (Knowledge)": [
    "OpenStack Nova 计算服务原理", 
    "控制节点与计算节点架构分工"
  ],
  "技能点实体 (Skill)": [
    "OpenStack 计算服务多节点部署能力",
    "控制节点与计算节点协同配置能力"
  ],
  "关联证书 (Certification)": ["华为HCCDA认证证书", "华为ICT大赛"]
}
```


## 证书数据（证）
```
{
  "证书唯一ID": "CERT_001",
  "证书/竞赛名称": "华为HCCDA认证证书",
  "发证/主办机构": "华为技术有限公司",
  "认证级别": "初级 (Associate)",
  "考核知识点 (Exam_Knowledge_Points)": [
    "云服务基础架构",
    "计算云服务配置",
    "网络云服务管理"
  ],
  "关联支撑岗位 (Target_Jobs)": [
    "云系统实施工程师",
    "云运维工程师"
  ]
}
```

# 本体构建
- **节点实体（Nodes）：**
    
    1. `Job`（岗位）：如“云计算网络研发工程师”
        
    2. `Course`（课程）：如“云计算平台管理”
        
    3. `Cert`（证书）：如“华为HCCDA认证证书”
        
    4. `Skill`（技能）：如“Docker”、“OpenStack 多节点部署”
        
    5. `Knowledge`（知识点）：如“Nova 计算服务原理”
        
- **关系连线（Relationships）：**
    
    - `[Job] -REQUIRE-> [Skill]` （岗位要求技能）
        
    - `[Course] -TEACH-> [Skill/Knowledge]` （课程传授技能/知识）
        
    - `[Cert] -EXAMINE-> [Knowledge]` （证书考核知识点）
        
    - **核心桥梁：** `[Course] -RELATED_TO-> [Cert]` （比如课的JSON里直接有关联证书，直接连线）

![[Pasted image 20260604143950.png|606]]
# 语义对齐
本地字典+云端API

## 向量化

大模型内部有一个极其庞大的多维空间（通常有几千个维度）。当我们把一个词（比如“Python”）输入给大模型提供的 Embedding 接口时，大模型不会返回文字，而是返回一串由浮点数组成的数组，比如： `[0.124, -0.456, 0.887, 0.002, ... ]`

这串数字，就是这个词在多维空间里的**坐标**。大模型在预训练时已经读过了全网的文章，它知道“k8s”和“容器编排”经常在同一类技术文档里出现，所以它会把这两个词的坐标分配得非常近。

### 本地字典：向量知识库

名为 `embedding_cache.json` 的纯文本文件。结构是一个巨大的键值对（Key-Value）

python处理（Set 化）
把岗位 JSON 里的 ==`Hard_Skills`==、课程 JSON 里的 ==`技能点实体`==、证书 JSON 里的 ==`考核知识点`== 全部提取出来，倒进一个大池子里。利用 Python 的 `set()`（集合）功能自动去重。（比如 500 个岗位都写了“Linux”，去重后就只剩 1 个“Linux”）。

### 调用大模型向量化

得到set后，调用大模型API获取坐标

==deepseek的API即可支持向量化==

# 知识图谱实现

工具：Neo4j
## 图谱结构
### 一、 图谱中的“点”（5大核心节点）

1. **岗位节点 (Job)：** 例如“云计算网络研发工程师”。（附带属性：薪资范围、城市、门槛等）
    
2. **课程节点 (Course)：** 例如“云计算平台管理”。
    
3. **证书节点 (Cert)：** 例如“华为HCCDA认证证书”。
    
4. **实践技能节点 (Skill)：** 包含企业要求的“硬技能”（如 Kubernetes）和课程培养的“实操能力”（如 OpenStack多节点部署）。
    
5. **理论知识节点 (Knowledge)：** 包含课程讲授的“理论知识”（如 Nova原理）和证书考核的“考点”（如 云服务基础架构）。
    

### 二、 图谱中的“线”（4种核心关系）

节点之间由不同类型的边连接，分为“硬链接（事实）”和“软链接（AI计算）”：

1. **(REQUIRE) 需求边 [硬链接]：** `[岗位] → [技能]`。说明该岗位明确要求某项技术。
    
2. **(TEACH) 传授边 [硬链接]：** `[课程] → [技能/知识点]`。说明这门课涵盖了哪些具体内容。
    
3. **(EXAMINE) 考核边 [硬链接]：** `[证书] → [知识点]`。说明考这个证需要掌握哪些考点。
    
4. **(ALIGN) 语义等价边 [软链接]：** ==使用余弦相似度计算！==它分为两层：
    
    - **产教融合桥：** `[岗位技能] ↔ [课程技能]` （例如：Docker ↔ 容器化技术）
        
    - **课证融通桥：** `[证书考点] ↔ [课程知识点]` （例如：云服务基础架构 ↔ 虚拟化技术基础原理）
    - **证书和岗位** `[证书考点] ↔ [岗位技能]` 
## neo4j 实现

1.  将节点和边，以及得到的向量值作为embedding写入
2.  调用函数计算“软链接”