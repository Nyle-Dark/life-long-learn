### 一、 字段级处理映射与规则清单

| **原始 CSV 字段** | **目标 JSON 字段**                  | **处理规则说明**                                                                  | **处理手段**    |
| ------------- | ------------------------------- | --------------------------------------------------------------------------- | ----------- |
| **职位ID**      | `岗位ID`                          | 直接保留，作为知识图谱中该岗位节点的唯一主键。                                                     | 纯 Python    |
| **岗位名称**      | `岗位名称`                          | 直接保留。                                                                       | 纯 Python    |
| **城市**        | `城市`                            | 直接保留，可用于后续的地域维度分析。                                                          | 纯 Python    |
| **学历要求**      | `门槛要求.学历`                       | 嵌套归类。遇空值填“不限”。                                                              | 纯 Python    |
| **经验要求**      | `门槛要求.经验`                       | 嵌套归类。遇空值填“不限”。                                                              | 纯 Python    |
| **薪资范围**      | `薪资特征`                          | **重构**。保留原始字符串，同时使用正则提取数字，转换为“薪资下限”和“薪资上限”（单位统一为 K）。                        | 纯 Python    |
| **关键技能**      | `核心硬技能`<br><br>  <br><br>`宽泛领域` | **核心重构**。将长字符串切割为数组；过滤掉“经验”、“专业”等无用词汇；将剩余词汇在语义上区分为“具体技术（硬技能）”和“业务方向（宽泛领域）”。 | **大模型 API** |
| **公司名称**      | `[丢弃]`                          | 避免隐私合规问题，且与技能拓扑图无关，直接删除。                                                    | 纯 Python    |
| **公司规模**      | `[丢弃]`                          | 对构建认知路径无直接计算价值，直接删除。                                                        | 纯 Python    |
| **公司领域**      | `[丢弃]`                          | 招聘平台分类较杂，信息熵低，直接删除。                                                         | 纯 Python    |
| **发布时间**      | `[丢弃]`                          | 我们采用截面数据分析，且爬取时间基本一致，直接删除。                                                  | 纯 Python    |
| **爬取时间**      | `[丢弃]`                          | 同上，为大模型推理减负，直接删除。                                                           | 纯 Python    |
### 二、 具体执行策略

#### 1. 纯 Python 脚本


- **清洗与丢弃：** 使用 Python 的 `csv` 库读取原表，在代码中只提取你需要的 6 个字段，其余 6 个字段直接忽略，实现数据“瘦身”。
    
- **数值化转换（正则表达式）：** 针对“薪资范围”（如 `8-13K` 或 `200-400元/天`），在 Python 中写一段 `re.findall(r'\d+', salary)` 的函数。如果是“天”，用代码乘以 22 天换算成 K。这完全是 Python 的强项，不需要 API 参与。
    

#### 2. 大模型 API 仅负责“关键技能”的语义分类

只有在处理“关键技能”这一列时，才需要向大模型发送请求。

- **规则：** 遍历到某一行的“关键技能”时（例如提取出字符串 `"功能测试, k8s, openstack, 通信相关专业"`），将这段纯文本作为 Prompt 变量，通过 `requests` 库发给 DeepSeek 或 GLM 的 API。
    
- **API 的任务：** 你的 Prompt 只需要规定一件事：==“请剔除包含‘专业’的废话，把具体的框架工具（k8s, openstack）放入 `Hard_Skills` 数组，把宏观方向（功能测试）放入 `Domains` 数组。”==
    
- **接收与合并：** API 会返回一个小型的 JSON 结构，你的 Python 脚本接收到这个结果后，把它和你前面提取的 ID、名称、薪资等信息拼装成一个大字典。
    

#### 3. 最终组装与输出

所有行遍历、清洗、拼接完毕后，Python 脚本执行 `json.dump()`，将所有组装好的字典导出为一个巨大的 `.json` 文件。

这样分工，既保证了数据基础结构的绝对精准（Python 兜底），又实现了非结构化标签的智能化分类（大模型发力），是最高效、最不易报错的工程落地路径。

# deepseek API

key：sk-9520f5b9254349e589e2122afdc31aa0

# 最终输出格式-json

```
{
  "岗位ID": "06d49e273d96be8403153tu1GFVW",
  "岗位名称": "云计算网络研发工程师",
  "城市": "武汉",
  "门槛要求": {
    "学历": "本科",
    "经验": "1-3年"
  },
  "薪资特征": {
    "原始区间": "15-22K",
    "薪资下限": 15,
    "薪资上限": 22
  },
  "核心硬技能 (Hard_Skills)": [
    "Golang",
    "C++",
    "Kubernetes",
    "Docker",
    "Linux",
    "云原生"
  ],
  "宽泛领域 (Domains)": [
    "数据库",
    "网络技术"
  ]
}